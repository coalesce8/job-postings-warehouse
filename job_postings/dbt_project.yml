name: job_postings

profile: job_postings

seed-paths: ["seeds"] 
model-paths: ["models"]
macro-paths: ["macros"]
clean-targets:
  - "target"
  - "dbt_packages"

models:
  job_postings:
    staging:
      +materialized: view

