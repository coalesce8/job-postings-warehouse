job_postings:              # must match profile: from step 1
  target: dev
  outputs:
    dev:
      type: duckdb
      path: "{{ env_var('JOBS_ANALYTICS_DB_PATH') }}"
      attach: 
        - path: "{{ env_var('JOBS_RAW_DB_PATH') }}"
          alias: raw
          read_only: true